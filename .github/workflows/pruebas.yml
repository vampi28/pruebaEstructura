name: Pruebas Automáticas y Clonado

on:
    push:
      branches:
        - development

jobs:
  build_and_clone:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código fuente
      uses: actions/checkout@v3

    - name: Configurar Git
      run: |
        git config --global user.email "vampero18@gmail.com"
        git config --global user.name "vampi28"

    - name: Configurar Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Instalar dependencias
      run: npm install

    - run: git branch -r

  #  - name: Ejecutar pruebas
  #    run: npm test

    - name: Si pasa las pruebas ejecuta una segunda
      if: success()
      steps:
        - name: Incluir Flujo de Trabajo 1
          uses: actions/checkout@v2
          with:
            ref: development
        - name: Flujo de Trabajo 1
          run: echo "Ejecutando Flujo de Trabajo 1"


